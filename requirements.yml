name: NPG
channels:
  - defaults
dependencies:
  - scipy
  - pandas
  - tqdm
  - jupyter
  - ipython
  - pytest
  - matplotlib
  - deepdiff
  - setuptools
  - wheel
prefix: /home/rodrigo/anaconda3/envs/NPG
